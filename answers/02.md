# Task 02 Answer
运行chmod +x tools/recruit-info从而添加可执行权限
./tools/recruit-info可执行是因为使用相对路径，指示明确；recruit-info只会在PATH中寻找，容易失败。
ls在PATH中，shell可以找到ls并运行。
export PATH="$PATH:$(pwd)/tools"将recruit-info暂时加入PATH中从而能执行。
